export interface CreateBuildingRequest {
  title: string;
  city: string;
  county: string;
  postcode: string;
  address: string;
}

export interface UpdateBuildingRequest {
  title: string;
  city: string;
  county: string;
  postcode: string;
  address: string;
}